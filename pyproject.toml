[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "do-smp"
version = "0.1.0"
description = "DESC Orchestrator for Scene Modeling Photometry"
readme = "README.md"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
